---
phase: 04.3-multi-tab-orchestration-with-background-streaming
verified: 2026-03-28T12:15:00Z
status: passed
score: 16/16 must-haves verified
re_verification: true
re_verification_meta:
  previous_status: passed
  previous_score: 16/16
  previous_verified: 2026-03-28T11:40:00Z
  trigger: "Plan 04 gap closure (unread indicator null-tabKey fix) landed AFTER initial verification was written; re-verification confirms fix is in place"
  gaps_closed:
    - "Unread indicator now targets real tab ID — ChatPanel extraction eliminates 'none' tabKey when activeTabId is null"
  gaps_remaining: []
  regressions: []
---

# Phase 04.3: Multi-tab Orchestration with Background Streaming — Verification Report

**Phase Goal:** Multiple concurrent CLI streams running in background across tabs, tab switching without losing state, status indicators (streaming/unread/error/tool-input), smart scroll with per-tab position preservation, soft concurrency limit with warning banner, draft preservation across tab switches.
**Verified:** 2026-03-28T12:15:00Z
**Status:** passed
**Re-verification:** Yes — Plan 04 gap closure executed after initial verification; re-verification confirms fix is present and no regressions introduced.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | tabStatusAtom derives 5-state priority: error > tool-input > unread > streaming > idle | VERIFIED | `src/atoms/chat.ts` lines 80-92 — exact priority order in computed atom; 7 tests in `chat.test.ts` cover all states and precedences |
| 2 | unreadAtom is set to true when AssistantMessageEvent arrives on a non-active tab | VERIFIED | `sendMessageAtom` lines 172-175 — compares `String(activeTab) !== tabId` after AssistantMessageEvent; only on completed messages, not streaming deltas |
| 3 | toolInputAtom is set to true when AssistantMessageEvent contains ToolUseBlock content | VERIFIED | `chat.ts` lines 155-160 — `block.type === "tool_use"` check inside `isAssistantMessage` branch |
| 4 | activeStreamCountAtom increments on stream start, decrements on finish/error | VERIFIED | `chat.ts` line 121 increments; `Effect.ensuring` block lines 183-190 decrements with `Math.max(0, ...)` guard |
| 5 | Error atom cleared on new message send, toolInputAtom cleared on stream resume | VERIFIED | `chat.ts` lines 116-117 clear `errorAtom` and `toolInputAtom` at start of `sendMessageAtom`; also cleared on `SystemInitEvent` (line 143) and `ResultEvent` (line 178) |
| 6 | CSS custom properties for status-tool-input and warning banner colors exist | VERIFIED | `src/index.css` lines 86-89 (`:root`), lines 124-127 (`.dark`), lines 47-50 (`@theme inline`) |
| 7 | Each tab preserves its scroll position when switching away and restores on switch back | VERIFIED | `index.tsx` — scroll event listener saves `el.scrollTop` to `scrollPositionAtom`; tab-switch `useEffect` on `tabKey` restores `el.scrollTop = savedScrollPos` |
| 8 | Auto-scroll enabled at bottom, disabled when user scrolls up | VERIFIED | `index.tsx` — `AT_BOTTOM_THRESHOLD = 32`; scroll handler sets `setAutoScroll(atBottom)` reactively |
| 9 | Scrolling to bottom re-enables auto-scroll AND clears unread | VERIFIED | `index.tsx` lines 87-89 — `setUnread(false)` inside `atBottom` check in scroll handler |
| 10 | Tab switch is instant with no animation | VERIFIED | No transition/animation CSS in `index.tsx`; tab content swap via atom subscription is synchronous |
| 11 | Per-tab draft input text is preserved when switching tabs | VERIFIED | `draftInputAtom(tabKey)` atom-backed input with `Atom.keepAlive`; survives tab switch; no TanStack Form present |
| 12 | New streaming content auto-scrolls only when auto-scroll is enabled | VERIFIED | `index.tsx` lines 107-112 — `if (!autoScroll) return` guard before `el.scrollTop = el.scrollHeight` |
| 13 | Session status indicator renders orange dot for tool-input state | VERIFIED | `session-status-indicator.tsx` line 19 — `status === "tool-input" && "bg-status-tool-input"` |
| 14 | Session status indicator shows no dot for idle state | VERIFIED | `session-status-indicator.tsx` line 10 — `if (status === "idle") return null` |
| 15 | Concurrency warning banner appears when 5+ streams are active, auto-dismisses below 5 | VERIFIED | `concurrency-warning-banner.tsx` — `CONCURRENCY_WARNING_THRESHOLD = 5`; `if (count < CONCURRENCY_WARNING_THRESHOLD) return null` |
| 16 | Banner is non-blocking inline element at top of sidebar content | VERIFIED | `app-sidebar.tsx` line 188 — `<ConcurrencyWarningBanner />` first child inside `<SidebarContent>`, before project groups |

**Score:** 16/16 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/atoms/chat.ts` | Extended TabStatus type, 4 new atom families, activeStreamCountAtom, updated sendMessageAtom | VERIFIED | 5-state `TabStatus` type; `unreadAtom`, `toolInputAtom`, `draftInputAtom`, `activeStreamCountAtom` all exported; `sendMessageAtom` has full unread/tool-input/concurrency logic |
| `src/atoms/scroll.ts` | scrollPositionAtom and autoScrollAtom families | VERIFIED | Both atoms exported with `Atom.keepAlive`; 9-line file, substantive and minimal |
| `src/atoms/__tests__/chat.test.ts` | Tests for 5-state tabStatusAtom derivation, new atom defaults | VERIFIED | 7 priority derivation tests + 4 default value tests for new atoms; all 181 tests pass |
| `src/atoms/__tests__/scroll.test.ts` | Tests for scroll atom defaults and isolation | VERIFIED | 3 tests covering default values and per-tab isolation |
| `src/index.css` | CSS custom properties for tool-input and warning colors | VERIFIED | Tokens present in `:root`, `.dark`, and `@theme inline` blocks |
| `src/routes/index.tsx` | Smart scroll behavior, draft input preservation, scroll position save/restore, null-safe tabKey via ChatPanel extraction | VERIFIED | `AT_BOTTOM_THRESHOLD = 32`, `scrollContainerRef`, 3 `useEffect` hooks for scroll/restore/auto-scroll, `draftInputAtom`, no `useForm`; `IndexComponent` guards null `activeTabId` and renders `<ChatPanel tabKey={String(activeTabId)} />` |
| `src/components/session-status-indicator.tsx` | 5-state status indicator with tool-input orange dot | VERIFIED | `SessionStatus` type includes `"tool-input"`; `bg-status-tool-input` class applied |
| `src/components/concurrency-warning-banner.tsx` | Inline sidebar banner for concurrency warnings | VERIFIED | `activeStreamCountAtom` subscribed via `useAtomValue`; threshold of 5; warning text "Performance may degrade with {count} active streams" |
| `src/components/app-sidebar.tsx` | Sidebar with ConcurrencyWarningBanner integrated | VERIFIED | `<ConcurrencyWarningBanner />` rendered as first child of `<SidebarContent>` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/atoms/chat.ts` | `tabStatusAtom` | `Atom.make` computed derivation with 4-input priority logic | VERIFIED | Lines 80-92: `err !== null` → error, `toolInput` → tool-input, `unread` → unread, `streaming` → streaming, else idle |
| `src/atoms/chat.ts` | `sendMessageAtom` | `ctx.set` calls for unread/toolInput/activeStreamCount | VERIFIED | `unreadAtom` set at line 174; `toolInputAtom` set at lines 117, 143, 159, 178; `activeStreamCountAtom` incremented at 121 and decremented in `Effect.ensuring` at 186-189 |
| `src/routes/index.tsx` | `src/atoms/scroll.ts` | `useAtomValue`/`useAtomSet` for scroll atoms | VERIFIED | Lines 66-69 import and subscribe to `autoScrollAtom` and `scrollPositionAtom` for current tab |
| `src/routes/index.tsx` | `src/atoms/chat.ts` | `useAtomSet(unreadAtom)` for clearing | VERIFIED | Line 70 `setUnread = useAtomSet(unreadAtom(tabKey))`; used in scroll handler at line 88 |
| `src/routes/index.tsx` | `src/atoms/chat.ts` | `useAtomValue(draftInputAtom)` | VERIFIED | Lines 62-63 read and write `draftInputAtom(tabKey)` |
| `src/routes/index.tsx` | `src/atoms/sidebar.ts` | `activeTabIdAtom` in `IndexComponent` for null-safe tabKey | VERIFIED | Line 34 reads `activeTabIdAtom`; null guard at line 36; `<ChatPanel tabKey={String(activeTabId)} />` at line 46 |
| `src/components/session-status-indicator.tsx` | `src/atoms/chat.ts` | `SessionStatus` type alignment with `TabStatus` | VERIFIED | Both types are `"streaming" | "unread" | "error" | "tool-input" | "idle"` |
| `src/components/concurrency-warning-banner.tsx` | `src/atoms/chat.ts` | `useAtomValue(activeStreamCountAtom)` | VERIFIED | Line 9 subscribes to `activeStreamCountAtom` from `@/atoms/chat` |
| `src/components/app-sidebar.tsx` | `src/components/concurrency-warning-banner.tsx` | JSX inclusion before project groups | VERIFIED | Line 35 import; line 188 `<ConcurrencyWarningBanner />` inside `<SidebarContent>` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `concurrency-warning-banner.tsx` | `count` | `activeStreamCountAtom` via `useAtomValue` | Yes — atom incremented/decremented by `sendMessageAtom` fibers via `Effect.ensuring` | FLOWING |
| `session-status-indicator.tsx` | `status` prop | `tabStatusAtom(tabKey)` computed from `errorAtom`, `toolInputAtom`, `unreadAtom`, `isStreamingAtom` | Yes — all 4 source atoms written in `sendMessageAtom` event loop | FLOWING |
| `src/routes/index.tsx` | `draft` | `draftInputAtom(tabKey)` via `useAtomValue` | Yes — written by user input via `setDraft(e.target.value)`; persists across tab switches via `Atom.keepAlive` | FLOWING |
| `src/routes/index.tsx` | `autoScroll` | `autoScrollAtom(tabKey)` via `useAtomValue` | Yes — written by scroll handler on DOM scroll events | FLOWING |
| `src/routes/index.tsx` | `savedScrollPos` | `scrollPositionAtom(tabKey)` via `useAtomValue` | Yes — written by scroll handler on each scroll event | FLOWING |
| `src/routes/index.tsx` (ChatPanel) | `tabKey` | `activeTabIdAtom` in outer `IndexComponent` | Yes — guaranteed to be a real numeric string; never "none"; null guard prevents ChatPanel mounting | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED — phase produces a React/Electron UI; no runnable CLI entry points testable without starting the Electron app.

---

### Requirements Coverage

The D-01 through D-16 identifiers in this phase are implementation decisions defined in `04.3-CONTEXT.md`, not rows in `.planning/REQUIREMENTS.md`. The REQUIREMENTS.md file uses different requirement ID prefixes (PROJ, GIT, ATOM, UI) for other phases. No D-XX entries appear in REQUIREMENTS.md for phase 04.3, so there are no orphaned requirements.

| Decision | Plan(s) | Description | Status |
|----------|---------|-------------|--------|
| D-01 | 04.3-01 | Background streaming continues on tab switch | SATISFIED — `sendMessageAtom` forks independent fibers; atom state updates regardless of active tab |
| D-02 | 04.3-01 | Enabled by centralized effect-atom state, no pause/resume logic | SATISFIED — `Atom.family` with `Atom.keepAlive` and global `sendMessageAtom` |
| D-03 | 04.3-01, 04.3-03 | Four status indicators (streaming/unread/error/tool-input) as colored dots | SATISFIED — 5-state `tabStatusAtom`, `SessionStatusIndicator` with all 4 colors |
| D-04 | 04.3-01 | Status indicators derive from atom state | SATISFIED — `tabStatusAtom` is a computed derived atom reading 4 source atoms |
| D-05 | 04.3-01, 04.3-03 | Error supersedes unread; full priority order | SATISFIED — priority: error > tool-input > unread > streaming > idle in `tabStatusAtom` |
| D-06 | 04.3-01, 04.3-03 | No indicator for idle state | SATISFIED — `if (status === "idle") return null` in `SessionStatusIndicator` |
| D-07 | 04.3-01, 04.3-04 | Unread only set on complete AssistantMessageEvent on non-active tab | SATISFIED — set inside `isAssistantMessage` branch, not streaming delta; compares to `activeTabIdAtom`; null-tabKey bug fixed in Plan 04 via ChatPanel extraction |
| D-08 | 04.3-02 | Unread clears when user scrolls to bottom | SATISFIED — `setUnread(false)` inside `atBottom` check in scroll handler; Plan 04 explicitly preserved this behavior |
| D-09 | 04.3-01 | Error persists until new message send | SATISFIED — `ctx.set(errorAtom(tabId), null)` only at start of `sendMessageAtom` |
| D-10 | 04.3-01, 04.3-03 | Soft limit warning at 5 concurrent streams | SATISFIED — `CONCURRENCY_WARNING_THRESHOLD = 5`; `activeStreamCountAtom` tracked with `Effect.ensuring` |
| D-11 | 04.3-03 | Warning as inline non-blocking sidebar banner | SATISFIED — `<ConcurrencyWarningBanner />` inline in `<SidebarContent>` |
| D-12 | 04.3-01, 04.3-03 | Banner auto-dismisses when count drops below 5 | SATISFIED — reactive `useAtomValue(activeStreamCountAtom)` with `return null` when below threshold |
| D-13 | 04.3-02 | Each tab preserves scroll position | SATISFIED — `scrollPositionAtom(tabKey)` with `Atom.keepAlive`; restored in tab-switch `useEffect` |
| D-14 | 04.3-02 | Smart auto-scroll at bottom, off on scroll up | SATISFIED — `AT_BOTTOM_THRESHOLD = 32`; `setAutoScroll(atBottom)` in scroll handler; guard in auto-scroll effect |
| D-15 | 04.3-02 | Instant tab swap, no animation | SATISFIED — no transition CSS; atom subscription swap is synchronous |
| D-16 | 04.3-02 | Per-tab draft input via Atom.family | SATISFIED — `draftInputAtom = Atom.family(...)` with `Atom.keepAlive`; TanStack Form removed from chat panel |

---

### Anti-Patterns Found

No blocking anti-patterns detected.

- No `TODO`, `FIXME`, or `PLACEHOLDER` comments in phase files
- No empty implementations — `return null` in `SessionStatusIndicator` (idle guard) and `ConcurrencyWarningBanner` (below-threshold hide) are correct conditional render patterns
- No hardcoded empty data arrays passed to renderers
- No orphaned artifacts — all new atoms are consumed by components or tests
- The `placeholder` match in `index.tsx` is an HTML `placeholder` attribute on an `<Input>` element, not a code stub
- TypeScript: no errors; advisory TS44 messages about `JSON.stringify` are pre-existing in `event-store` tests, unrelated to this phase

**Design note — unread dot color:** `session-status-indicator.tsx` renders the unread state as `bg-sidebar-primary`. In dark mode `sidebar-primary` is oklch(0.488 0.243 264.376) — an indigo/blue color, not the "solid green dot" described in D-03. In light mode it is near-black. This is a design deviation from the spec but is not a functional regression: the dot appears, changes state correctly, and clears on scroll-to-bottom. Flagged for human review.

---

### Human Verification Required

The following behaviors require manual testing in the running Electron app:

**1. Background streaming continuity**

**Test:** Open two sessions, start a query in Session A, switch to Session B before the response finishes, switch back to Session A.
**Expected:** Response text accumulated during the time on Session B is fully visible; no content lost.
**Why human:** Requires live Electron process with a real Claude CLI response.
**UAT result:** PASSED (confirmed in 04.3-HUMAN-UAT.md)

**2. Unread indicator appears on tab switch**

**Test:** Start a query in Session A, switch to Session B, wait for Session A to complete, observe sidebar.
**Expected:** Session A shows a dot (unread state) in the sidebar using `sidebar-primary` color; dot disappears after scrolling to bottom in Session A.
**Why human:** Requires running app with real concurrent streams. Plan 04 fix (ChatPanel extraction) addresses the root cause identified in UAT.
**UAT result:** ISSUE then FIXED — initial UAT failed ("no solid green dot"); root cause diagnosed as null activeTabId producing "none" tabKey; Plan 04 extracted ChatPanel to eliminate this; not re-run in live app post-fix (code fix is verifiable statically).

**3. Tool-input orange dot**

**Test:** Trigger a tool-use response from Claude CLI in one session.
**Expected:** Session entry shows orange dot while Claude is waiting for tool confirmation.
**Why human:** Requires real Claude CLI tool-use interaction.
**UAT result:** SKIPPED (requires tool confirmation setup).

**4. Concurrency warning banner at 5 streams**

**Test:** Open 5+ sessions and start queries in all of them simultaneously.
**Expected:** Yellow/orange inline banner appears at top of sidebar after the 5th stream starts; disappears as streams complete.
**Why human:** Requires 5 simultaneous live Claude CLI processes.
**UAT result:** SKIPPED (requires 5+ projects/sessions).

**5. Scroll position preservation on tab switch**

**Test:** In Session A, scroll up halfway, switch to Session B, switch back to Session A.
**Expected:** Session A scroll position is exactly where it was left.
**Why human:** Requires DOM scroll state verification in running app.
**UAT result:** PASSED (confirmed in 04.3-HUMAN-UAT.md).

**6. Unread dot color matches spec**

**Test:** Trigger an unread state on a tab in dark mode and light mode.
**Expected:** D-03 specifies a "solid green dot" for unread. Current implementation uses `bg-sidebar-primary` which is indigo in dark mode and near-black in light mode.
**Why human:** Color perception and design intent require visual inspection.

---

## Summary

Phase 04.3 is fully implemented across 4 plans. All 16 D-XX implementation decisions have verified code evidence across 9 source files.

**Gap closure confirmed:** Plan 04 (commit `c0824cb`) extracted `ChatPanel` from `IndexComponent` to eliminate the null-`activeTabId` → `"none"` tabKey bug. The outer `IndexComponent` now reads `activeTabIdAtom` and renders a placeholder when null; `ChatPanel` only mounts with a guaranteed real numeric tab ID, ensuring `unreadAtom("42")` not `unreadAtom("none")` is the target for `sendMessageAtom`. This fix was not present when the initial VERIFICATION.md was written.

Key verifications:

- 5-state `TabStatus` with correct error-first priority derivation in `src/atoms/chat.ts`
- 6 new atoms (`unreadAtom`, `toolInputAtom`, `draftInputAtom`, `activeStreamCountAtom`, `scrollPositionAtom`, `autoScrollAtom`) are substantive, exported, and consumed by downstream components
- `sendMessageAtom` correctly tracks all three behaviors (unread, tool-input, concurrency) with `Effect.ensuring` for leak-safe decrement
- Smart scroll implementation in `index.tsx` uses the correct 32px threshold, RAF-throttled passive listener, and atom-backed state
- `ConcurrencyWarningBanner` and `SessionStatusIndicator` are wired to their atom data sources and rendered in the sidebar
- `IndexComponent` guards null `activeTabId`; `ChatPanel` receives only validated string tab keys
- All 181 tests pass; no TypeScript errors; no blocking anti-patterns

One design deviation from spec: unread dot uses `sidebar-primary` color (indigo in dark mode) rather than a green, per D-03's description. Functionally correct but visually diverges from spec wording. Flagged for human review.

---

_Verified: 2026-03-28T12:15:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification: Yes — covers Plan 04 gap closure (commit c0824cb)_
