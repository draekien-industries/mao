---
phase: 04.5-fix-tab-working-directory-mismatch
verified: 2026-03-28T00:09:00Z
status: passed
score: 6/6 must-haves verified
gaps: []
human_verification:
  - test: "First message after app cold start spawns CLI in project directory"
    expected: "Claude CLI process runs with cwd set to the registered project directory, not the app default directory"
    why_human: "Requires running the Electron app, registering a project, sending a message, and confirming via tool use or file access that the working directory is correct. Cannot verify process spawn directory from static analysis alone."
---

# Phase 04.5: Fix Tab Working Directory Mismatch — Verification Report

**Phase Goal:** Thread each tab's stored working directory through to CLI process spawning so queries and resumes run in the correct project directory
**Verified:** 2026-03-28T00:09:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | New query messages spawn CLI in the tab's project directory, not the app default directory | VERIFIED | `sendMessageAtom` reads `ctx(cwdAtom(tabId))` and passes `cwd: cwd \|\| undefined` to `new QueryParams(...)` in `chat.ts` line 140; `service.ts` line 64 applies `Command.workingDirectory` when cwd is truthy |
| 2 | Resumed session messages spawn CLI in the tab's project directory | VERIFIED | Same `cwd` value passed to `new ResumeParams({ prompt, session_id: currentSessionId, cwd: cwd \|\| undefined })` in `chat.ts` lines 133-138 |
| 3 | Tab switch populates the cwd for the activated tab | VERIFIED | `setActiveTabAtom` in `sidebar.ts` lines 79-85: looks up tab from `projectsAtom`, calls `ctx.set(cwdAtom(String(tabId)), tab.cwd)` |
| 4 | First tab activation on app start populates cwd | VERIFIED | `loadProjectsAtom` in `sidebar.ts` line 62: `ctx.set(cwdAtom(String(firstTab.id)), firstTab.cwd)` — directly after `ctx.set(activeTabIdAtom, firstTab.id)` |
| 5 | Newly registered projects populate cwd for the auto-created tab | VERIFIED | `registerProjectAtom` in `sidebar.ts` line 168: `ctx.set(cwdAtom(String(tab.id)), directory)` |
| 6 | Newly created sessions populate cwd for the new tab | VERIFIED | `createSessionAtom` in `sidebar.ts` line 236: `ctx.set(cwdAtom(String(tab.id)), sessionCwd)` — uses the resolved worktree path, not the original project cwd |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/atoms/chat.ts` | `cwdAtom` family and cwd threading in `sendMessageAtom` | VERIFIED | Lines 67-70: `export const cwdAtom = Atom.family((_tabId: string) => Atom.make("").pipe(Atom.keepAlive))`. Lines 130, 137, 140: cwd read and passed to both param constructors |
| `src/atoms/sidebar.ts` | `cwdAtom` population in all four tab activation paths | VERIFIED | Import at line 3. Population at: `setActiveTabAtom` (line 84), `loadProjectsAtom` (line 62), `registerProjectAtom` (line 168), `createSessionAtom` (line 236) |
| `src/atoms/__tests__/chat.test.ts` | `cwdAtom` family stability and default value tests | VERIFIED | `cwdAtom` imported at line 5. Three tests: same-ref stability (line 37), different-ref isolation (line 43), default empty string (line 158) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/atoms/sidebar.ts` | `src/atoms/chat.ts` | `import { cwdAtom } from "@/atoms/chat"` | WIRED | Line 3 of `sidebar.ts`; `cwdAtom` used at 4 call sites in sidebar (lines 62, 84, 168, 236) |
| `src/atoms/chat.ts` (`sendMessageAtom`) | `QueryParams` / `ResumeParams` | `cwd: cwd \|\| undefined` passed to constructors | WIRED | Lines 137 and 140 of `chat.ts`; both constructors receive the resolved cwd value |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `sendMessageAtom` (`chat.ts`) | `cwd` | `ctx(cwdAtom(tabId))` | Yes — populated from `tab.cwd` in `projectsAtom`, which is loaded from DB via `listTabs` RPC | FLOWING |
| `setActiveTabAtom` (`sidebar.ts`) | `tab.cwd` | `projectsAtom` flat-mapped to find matching session | Yes — `projectsAtom` is populated by `loadProjectsEffect` which calls `client.listTabs({})` and maps to `Tab` schema rows with real `cwd` values | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — the core spawn behavior requires a running Electron app with an active CLI process. Static code analysis fully covers the wiring; the remaining gap is integration-level and routed to human verification.

### Requirements Coverage

The PLAN frontmatter lists CWD-01, CWD-02, CWD-03. The ROADMAP.md also references these three IDs for phase 04.5. However, **REQUIREMENTS.md does not define CWD-01, CWD-02, or CWD-03**. These IDs do not appear anywhere in `.planning/REQUIREMENTS.md`.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CWD-01 | 04.5-01-PLAN.md | Not defined in REQUIREMENTS.md | ORPHANED | ID appears in ROADMAP.md and PLAN frontmatter but has no entry in REQUIREMENTS.md |
| CWD-02 | 04.5-01-PLAN.md | Not defined in REQUIREMENTS.md | ORPHANED | Same as CWD-01 |
| CWD-03 | 04.5-01-PLAN.md | Not defined in REQUIREMENTS.md | ORPHANED | Same as CWD-01 |

**Note:** The implementation fully satisfies the intent of these requirements as described in CONTEXT.md and the plan's must-haves. The gap is documentation-only — the CWD-* IDs were introduced in ROADMAP.md for this inserted phase but were never added to REQUIREMENTS.md. The phase goal is achieved regardless; REQUIREMENTS.md should be updated to register these IDs if requirement traceability is needed.

### Anti-Patterns Found

No blockers or warnings found.

- `cwd || undefined` coercion pattern at lines 137 and 140 of `chat.ts`: this is intentional — converts the empty-string default of `cwdAtom` to `undefined` so `Command.workingDirectory` is not applied when no cwd is set, preserving fallback behaviour.
- No TODO/FIXME/placeholder comments in modified files.
- No hardcoded empty returns in modified paths.
- No stub implementations.

### Human Verification Required

#### 1. CLI spawns in correct project directory on first message

**Test:** Register a project pointing to a known directory (e.g., a git repo). Send a message asking Claude to list files in the current directory (`ls` or `find .`). Observe whether the output reflects the registered project directory.
**Expected:** Claude's file/tool operations target the registered project directory, not the Electron app default directory.
**Why human:** Verifying the actual `cwd` of a spawned child process requires either running the app or inspecting process state at runtime. Static analysis confirms the wiring is correct but cannot substitute for an end-to-end execution check.

### Gaps Summary

No gaps. All six must-have truths are verified by direct code inspection. The full data flow is traceable from:

1. `Tab.cwd` stored in SQLite (pre-existing, not changed in this phase)
2. Loaded into `projectsAtom` via `loadProjectsEffect` → `client.listTabs` RPC
3. Written to `cwdAtom(tabId)` by all four sidebar activation paths
4. Read from `cwdAtom(tabId)` in `sendMessageAtom`
5. Passed as `cwd: cwd || undefined` to `QueryParams` / `ResumeParams`
6. Applied as `Command.workingDirectory(command, params.cwd)` in `service.ts` when cwd is truthy

The only open item is the **documentation gap**: CWD-01, CWD-02, CWD-03 are referenced in ROADMAP.md and the PLAN but are not registered in REQUIREMENTS.md. This does not affect phase goal achievement.

All 192 tests pass. Typecheck exits 0.

---

_Verified: 2026-03-28T00:09:00Z_
_Verifier: Claude (gsd-verifier)_
