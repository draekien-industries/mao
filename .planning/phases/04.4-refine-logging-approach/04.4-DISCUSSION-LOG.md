# Phase 04.4: Refine Logging Approach - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-28
**Phase:** 04.4-refine-logging-approach
**Areas discussed:** Production logging strategy, devLog consolidation, Log granularity & filtering, Log output format & destination

---

## Production Logging Strategy

### Q1: What should packaged (production) builds do with logs?

| Option | Description | Selected |
|--------|-------------|----------|
| Stay silent | Keep ProdLogger = Logger.none. No log output in packaged builds. | |
| Log to file | Write logs to a file in app.getPath('logs'). Enables post-mortem debugging. | |
| Log errors only to file | ProdLogger only captures Effect.logError calls to a file. Middle ground. | ✓ |

**User's choice:** Log errors only to file
**Notes:** None

### Q2: Should the error log file have rotation or size limits?

| Option | Description | Selected |
|--------|-------------|----------|
| Simple max-size truncate | Cap at fixed size (e.g., 5MB). Truncate oldest half when exceeded. | ✓ |
| You decide | Claude picks simplest approach preventing unbounded disk growth. | |
| No rotation | Append forever. Users manually delete. | |

**User's choice:** Simple max-size truncate
**Notes:** None

### Q3: Should production error logs include structured annotations or just plain text?

| Option | Description | Selected |
|--------|-------------|----------|
| Structured JSON | Each log line is a JSON object with timestamp, level, message, annotations. | ✓ |
| Pretty text | Human-readable formatted text similar to DevLogger output. | |
| You decide | Claude picks format that fits Effect Logger ecosystem. | |

**User's choice:** Structured JSON
**Notes:** None

### Q4: Should the prod error log file path be configurable?

| Option | Description | Selected |
|--------|-------------|----------|
| Always app.getPath('logs') | Standard Electron log directory. No config needed. | ✓ |
| Configurable via env var | Allow LOG_PATH override for power users. | |
| You decide | Claude picks simplest approach. | |

**User's choice:** Always app.getPath('logs')
**Notes:** None

---

## devLog Consolidation

### Q1: Should the devLog helper in main.ts be replaced with Effect logger calls?

| Option | Description | Selected |
|--------|-------------|----------|
| Replace with Effect logger | Use Effect.logInfo with annotations. Unifies all logging. | |
| Keep devLog for early lifecycle | Some lifecycle logs fire before runtime is ready. | |
| You decide | Claude picks based on Electron lifecycle timing. | |

**User's choice:** Other (free text)
**Notes:** "I think devLog is used where the effect runtime is not available. If we can make the runtime available where the dev logs are used then we should do that. Otherwise we need to keep the devLog around." — Claude investigated and confirmed 4 of 5 devLog calls are outside the Effect runtime's safe window.

### Q2: Given that 4 of 5 devLog calls are outside the Effect runtime's safe window, how should we handle this?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep devLog as-is | The 5 lifecycle logs are a special case. Not worth complexity for 2 migratable logs. | ✓ |
| Migrate the 2 mid-lifecycle logs | Move 'creating window' and 'app ready' to runtime.runSync. Keep devLog for rest. | |
| You decide | Claude picks based on cost/benefit. | |

**User's choice:** Keep devLog as-is
**Notes:** None

### Q3: Should devLog get a consistent naming pattern or annotation?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep [mao:lifecycle] prefix as-is | Current prefix is clear and grep-friendly. | |
| Export devLog from diagnostics.ts | Move helper to diagnostics.ts for reuse. Still console.log under the hood. | ✓ |
| You decide | Claude picks based on whether other files need it. | |

**User's choice:** Export devLog from diagnostics.ts
**Notes:** None

---

## Log Granularity & Filtering

### Q1: Should the app use Effect log levels (Debug, Info, Warning, Error)?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, use Debug for verbose | Move per-operation logs to logDebug. Keep lifecycle at logInfo. Errors at logError. | ✓ |
| Keep everything at Info+Error | Current two-tier approach. Simple. | |
| You decide | Claude picks level split for existing call sites. | |

**User's choice:** Yes, use Debug for verbose
**Notes:** None

### Q2: What should be at Debug level vs Info level?

| Option | Description | Selected |
|--------|-------------|----------|
| Debug = per-operation, Info = lifecycle | Debug: method entry/exit. Info: layer construction, runtime lifecycle. | ✓ |
| Debug = reads, Info = writes+lifecycle | Debug: read/query operations. Info: mutations and lifecycle. | |
| You decide | Claude assigns levels by signal-to-noise ratio. | |

**User's choice:** Debug = per-operation, Info = lifecycle
**Notes:** None

### Q3: Should the dev logger have a minimum level filter?

| Option | Description | Selected |
|--------|-------------|----------|
| Show all levels in dev | DevLogger with no level filter. All levels visible in console. | ✓ |
| Configurable min level | LOG_LEVEL env var or flag for filtering. | |
| You decide | Claude picks based on expected volume. | |

**User's choice:** Show all levels in dev
**Notes:** None

### Q4: Should renderer-side logs use the same Debug/Info/Error split?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, same split | Identical level conventions in renderer. Consistent across processes. | ✓ |
| Renderer stays Info+Error only | Keep renderer simpler since DevTools has its own filtering. | |
| You decide | Claude picks based on renderer log volume. | |

**User's choice:** Yes, same split
**Notes:** None

---

## Log Output Format & Destination

### Q1: Should dev builds also write logs to a file?

| Option | Description | Selected |
|--------|-------------|----------|
| Console only in dev | DevLogger writes to stdout/DevTools only. No file logging in dev. | ✓ |
| Both console and file in dev | Dev builds write all logs to file AND console. | |
| You decide | Claude picks based on complexity trade-off. | |

**User's choice:** Console only in dev
**Notes:** None

### Q2: For the production error log file, should it use Logger.json or a custom file-writing logger?

| Option | Description | Selected |
|--------|-------------|----------|
| Custom file logger using Logger.make | Build custom Logger filtering Error+ and appending JSON lines. Full control. | ✓ |
| Logger.json piped to file | Use built-in Logger.json and pipe stdout to file via Electron. Less control. | |
| You decide | Claude picks best integration with Effect Logger API. | |

**User's choice:** Custom file logger using Logger.make
**Notes:** None

### Q3: Should the production file logger be its own Effect service or a Logger constant?

| Option | Description | Selected |
|--------|-------------|----------|
| Logger constant in diagnostics.ts | Export ProdFileLogger alongside DevLogger/ProdLogger. No new service needed. | ✓ |
| Dedicated FileLogger service | Proper Effect service with acquireRelease for file handle. | |
| You decide | Claude picks based on file handle lifecycle. | |

**User's choice:** Logger constant in diagnostics.ts
**Notes:** None

---

## Claude's Discretion

- Exact max file size for log truncation
- Truncation implementation details
- Whether ProdLogger constant changes or new ProdFileLogger is composed alongside it
- devLog function signature when exported from diagnostics.ts

## Deferred Ideas

None — discussion stayed within phase scope
